#include "Header.h"
#include "tests.h"
const int n = 1000;
const int r = 1000;

int main() {
	runAllTests();
	/*int* A = new int[n * n];
	int* B = new int[n * n];

	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			A[i * n + j] = 1;
			B[i * n + j] = 1;
		}
	}

	int* C1 = canon(A, B, n);
	int* C2 = canon_external(A, B, n);
	int* C3 = canon_internal(A, B, n);*/
	/*for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			std::cout << C1[i * n + j] << " ";
		}
		std::cout << '\n';
	}*///;'';';';';/////////

	return 0;
}
